# Claude Opus 4.6 via OpenRouter

model:
  api_key: ${OPENROUTER_API_KEY}
  base_url: https://openrouter.ai/api/v1
  model_id: anthropic/claude-opus-4.6

judge:
  api_key: ${OPENROUTER_API_KEY}
  base_url: https://openrouter.ai/api/v1
  model_id: google/gemini-3-flash-preview
  enabled: true

defaults:
  trace_dir: traces_claude_opus_46
  tasks_dir: tasks
